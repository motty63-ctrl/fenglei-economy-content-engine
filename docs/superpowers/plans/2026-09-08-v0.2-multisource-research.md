# V0.2 Implementation Plan

1. 用 TDD 建立 artifact registry、owner 校验、hash 和 stale 传播。
2. 建立 SearchProvider 协议与 Tavily discovery adapter；provider 失败不落半成品。
3. 直接抓取原网页并保存 source documents/index。
4. 实现可信度排序、原始发布链和保守独立性去重。
5. 从已抓取正文抽取 evidence，生成 claim 级 `facts.json`。
6. 生成带 claim/source 引用链的 `research.md`。
7. 补 CLI、失败恢复、全量回归、离线示例和 Tavily 实网冒烟测试。
