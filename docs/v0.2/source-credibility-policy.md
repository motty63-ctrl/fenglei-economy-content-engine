# 来源可信度与独立性策略

默认排序：A 官方机构/原始数据；B 国际组织；C 公司原始披露；D 主流媒体；E 行业媒体；F 博客/公众号。

独立性按原始发布链而不是域名数量计算。同一新闻稿转载、聚合转载、同机构页面复用同一底层数据、或指向同一原始材料的结果只算一个来源。能识别 `original_url` 时归并到同一 `independence_key`；正文高度相同会触发保守归并。无法确认来源链的页面 `counts_as_independent=false`。公众号只作为线索，不可单独验证高风险事实。
