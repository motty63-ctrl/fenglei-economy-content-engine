# Fed SEP Revisions — Case State

- **Topic:** Federal Reserve SEP revisions (case label from `cases/fed-sep-revisions/`).
- **Approved checkpoint status:** The staging canary input used `checkpoint_schema_version=approved-checkpoint/1.0`. The envelope gate reported no approval-status error; its recorded failure was the missing formal `AngleCandidate` fields. The original checkpoint body is not present in the staging package, so its content cannot be independently reconstructed from these artifacts.
- **Current import status:** Staging only; not promoted. `.checkpoint-staging/cp-5ec4fb8d89137a2224d9/` contains `checkpoint_fingerprint.json` and `gates.json`. No materialized formal artifacts or Fed run are present under `runs/`.
- **Current blocker:** Checkpoint contract validation failed with `ANGLE_FORMAL_FIELDS_MISSING`. Missing fields are `audience_relevance`, `controversy_risk`, `core_insight`, `core_question`, `eligibility`, `evidence_strength`, `explainability`, `hook`, `hook_strength`, `novelty`, `total_score`, and `visual_potential`. Do not invent or default these values. Provenance, fact-coverage, and script-coverage gates remain false because validation stopped before provenance capture and materialization.
- **Next checkpoint:** Recover the actual required angle values from an existing approved checkpoint record and include them in the checkpoint input; do not infer or regenerate the angle. If the approved material does not contain them, keep the case blocked. Then rerun staging import and inspect schema, provenance, 100% fact-coverage, and script-coverage gates before creating a formal run.
